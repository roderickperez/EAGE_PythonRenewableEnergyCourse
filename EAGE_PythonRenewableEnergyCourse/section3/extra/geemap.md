# geemap

`geemap` links Jupyter maps with Google Earth Engine. Earth Engine authentication and project access are required. Always document dataset identifiers, spatial resolution, temporal coverage, cloud masking, scale, projection, and aggregation method; a satellite layer is not automatically analysis-ready.
