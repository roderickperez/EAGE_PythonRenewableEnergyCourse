# ipyleaflet

`ipyleaflet` provides interactive Leaflet maps as Jupyter widgets. It is useful when Python callbacks, layer controls, or notebook interaction are required. Widget output depends on a compatible Jupyter front end, so provide a static fallback for exported books.
