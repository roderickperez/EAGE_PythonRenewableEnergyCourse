/* EAGE Python SandBox — textos en español. */
window.PYTHONLAB_SPRAK = window.PYTHONLAB_SPRAK || {};
window.PYTHONLAB_SPRAK.es = {
  "kod": "es",
  "namn": "Español",
  "html_lang": "es",
  "titel": "EAGE Python SandBox",
  "beskrivning": "Un entorno interactivo de Python para la formación de EAGE en energías renovables.",
  "ui": {
    "nytt_filnamn": "new_program.py",
    "ny": "Nuevo",
    "aterstall": "Restablecer",
    "oppna": "Abrir archivo",
    "spara": "Guardar",
    "spara_som": "Guardar como",
    "kor": "Ejecutar",
    "om": "Acerca de EAGE",
    "start": "Inicio",
    "kapitel_rubrik": "Capítulos",
    "proyectos": "Proyectos",
    "flik_kapitel": "Capítulo",
    "flik_utskrift": "Salida"
  },
  "steg": {
    "kalla": "Código fuente",
    "tolk": "Análisis",
    "kor": "Ejecución",
    "resultat": "Resultado"
  },
  "konsol": {
    "startar": "Python se está descargando en su navegador. La primera vez toma unos segundos."
  },
  "ord": {
    "tips": "Sugerencia: ",
    "kapitel_rubrik": "Capítulo {nr} — {titel}",
    "rensa": "Limpiar salida"
  },
  "status": {
    "hamtar_paket": "Obtener ",
    "startar": "Iniciando Python...",
    "hamtar": "Obtener Python de {kalla} ...",
    "kontroll": "Obtener paquetes ...",
    "redo": "Python está listo.",
    "misslyckades": "Python no pudo iniciar"
  },
  "ladd": {
    "rubrik": "Iniciando Python...",
    "text": "Preparación",
    "not": "Python se está descargando en su navegador, unos 10 MB. La primera vez toma un momento — después de que se cachea y comienza rápidamente.",
    "hamtar": "Obteniendo Python",
    "kontaktar": "Contacto con {kalla}",
    "laddar_ner": "Descargando ...",
    "av": "{gjort} de {totalt}",
    "hamtat": "{gjort} descargado",
    "nedladdning_klar": "Descarga completa",
    "startar": "Iniciando Python",
    "packar": "Desembalaje y puesta en marcha del intérprete: la página puede congelarse brevemente",
    "nastan": "Casi hecho.",
    "forbereder": "Preparación del laboratorio",
    "verktyg": "Conectando las herramientas del laboratorio",
    "igen": "Inténtalo de nuevo.",
    "byter": "Cambiando de fuente",
    "klart": "Hecho",
    "redo": "Python está listo.",
    "misslyckades": "Python no pudo iniciar",
    "ingen_kalla": "Ninguna de las fuentes respondió",
    "ladda_om": "Recarga la página e inténtalo de nuevo."
  },
  "msg": {
    "syntaxfel": "Error de sintaxis en la línea {rad}, carácter {kolumn}: {text}",
    "hamtning_misslyckades": "no se pudo obtener ",
    "tidsgrans": "Tiempo muerto: ",
    "sparad_hamtade": "Guardado como {namn} en su carpeta Descargas.",
    "sparad_till": "Guardado en {namn}.",
    "oppnade": "Se abrió {namn} en {plats}.",
    "paketfel": "No se pudo obtener un paquete: ",
    "matplotlibfel": "No se pudo configurar matplotlib: ",
    "ingen_utskrift": "El programa se ejecutó sin imprimir nada. Utilice print(...) para ver el resultado.",
    "klart": "[terminado en {ms} ms]",
    "forsoker_hamta": "Tratando de obtener Python de {kalla} ...",
    "gick_inte": "Falló: ",
    "ingen_kalla": "Ninguna de las fuentes respondió. Volver a cargar la página e intentar de nuevo. Si eso no ayuda, la descarga está bloqueada aquí — entonces es hora de instalar Python en su computadora en su lugar.",
    "redo": "Python está listo. Escoja un capítulo a la izquierda, o escriba algo propio y presione Ejecutar."
  },
  "fraga": {
    "toma": "Todo lo que escribiste en {plats} se perderá.",
    "aterstall": "¿Restaurar el código original para {plats}? Cualquier cosa que escriba o cargue se perderá.",
    "radera_fil": "¿Borrar {namn}?",
    "filnamn": "¿Cómo se debe llamar el archivo?"
  },
  "feltips": {
    "KeyboardInterrupt": "Detuviste el programa, presiona Ejecutar para empezar de nuevo.",
    "SyntaxError": "Un error en la sintaxis del lenguaje. Compruebe que cada paréntesis, corchete y comilla tenga su pareja, y que haya dos puntos (:) después de if, for, while y def.",
    "IndentationError": "La sangría no coincide. Las líneas que pertenecen al mismo bloque bajo if, for o def necesitan exactamente cuatro espacios delante y la misma sangría entre sí.",
    "NameError": "Python no reconoce el nombre. O se escribe de manera diferente en dos lugares, o la variable no se ha creado todavía — o falta una importación.",
    "TypeError": "Usted está mezclando dos tipos de datos que no van juntos, por ejemplo texto y números. Pruebe int(...) o str(...) para convertir.",
    "ValueError": "El valor no encajaba. Por ejemplo: int(\"hello\") no funciona, porque \"hola\" no es un número.",
    "ZeroDivisionError": "No se puede dividir por cero, ni siquiera una computadora puede.",
    "IndexError": "Usted está pidiendo una posición en la lista que no existe. Recuerde que la primera posición es 0 y la última es len(list) - 1.",
    "KeyError": "Esa clave no está en su diccionario. Pruebe .get(key, fallback) si podría faltar.",
    "AttributeError": "Ese tipo no tiene tal método. Compruebe la ortografía, y que la variable contiene lo que usted piensa que hace.",
    "RecursionError": "Una función se llama a sí misma sin llegar nunca al caso base. Compruebe que el argumento realmente se encoge hacia la condición de parada.",
    "ModuleNotFoundError": "Ese módulo no está disponible en el Python del navegador. La biblioteca estándar funciona (random, math, time, string, json), pero pygame y otros módulos similares no."
  },
  "py": {
    "cors": "No se pudo llegar a {url}. O el servidor está caído, o no permite a los navegadores obtener datos de otros sitios (CORS). Error original: {fel}",
    "statuskod": "El servidor respondió con código de estado {kod}",
    "avbrot_input": "La entrada fue cancelada.",
    "ingen_fil": "No se ha seleccionado ningún archivo.",
    "saknas": "El archivo {namn} no existe. ¿Lo creó primero con open()?",
    "tom_lista": "La lista está vacía, no hay nada que guardar.",
    "laddade_ner": "Descargado {namn} (caracteres {tecken})",
    "laste_in": "Leer {fran} -> {till} (caracteres {tecken})",
    "inga_filer": "Todavía no hay ningún archivo. Cree uno con open(..., \"w\")."
  },
  "startkod": [
    "# ¡Bienvenidos a EAGE Python SandBox!",
    "# Presione Ejecutar (o Ctrl + Enter) y vea qué sucede.",
    "",
    "print(\"Hello world!\")",
    ""
  ],
  "kapitel": [
    {
      "del": "Fundamentos",
      "titel": "Impresión",
      "fil": "01_printing.py",
      "kod": "# Todo después de un # es un comentario.\n# Python no lo lee — está ahí para los humanos.\n\nprint(\"Hi! My name is Python.\")\nprint(\"I do exactly what you tell me to.\")\n\nprint(\"A\", \"B\", \"C\")            # comas ponen un espacio entre\nprint(\"A\", \"B\", \"C\", sep=\"\")    # sep decide lo que va entre\nprint(\"Loading\", end=\"\")         # fin decide lo que viene último\nprint(\"...done!\")\n",
      "forklaring": "\n      <h3>Qué es un programa</h3>\n      <p>Un programa es una lista de instrucciones que la computadora lleva a cabo de arriba a abajo, una línea a la vez. Nunca adivina y nunca se salta nada. Es por eso que la programación se puede aprender: la computadora hace precisamente lo que escribió, incluso cuando está mal.</p>\n      <h3>La sintaxis de la impresión</h3>\n      <p><code>print</code> es el nombre de la función. Los paréntesis después de que significa \"corre esto ahora\", y lo que va dentro se llama un <em>argumento</em> - lo que pasas.</p>\n      <ul>\n        <li><code>\"Hi\"</code> entre comillas es un <strong>cadena</strong>, es decir, texto. Las citas no son parte del texto; marcan donde comienza y termina.</li>\n        <li>Usted puede utilizar cualquiera de los dos <code>\"...\"</code> o <code>'...'</code>, siempre y cuando uses lo mismo en ambos extremos.</li>\n        <li>Varios argumentos están separados por comas. <code>print</code> entonces pone un espacio entre ellos automáticamente.</li>\n      </ul>\n      <h3>Argumentos designados</h3>\n      <p><code>sep=\"\"</code> y <code>end=\"\"</code> son <em>argumentos nombrados</em>: configuración adicional que se da con un signo igual. <code>sep</code> es lo que termina <em>entre</em> los elementos, <code>end</code> es lo que viene <em>después</em> Normalmente todo. <code>end</code> es un salto de línea, por lo que cada uno <code>print</code> empieza en una nueva línea.</p>\n      <div class=\"fallgrop\"><strong>Error común:</strong> si olvida una comilla o un paréntesis, obtendrá <code>SyntaxError</code>. Python solo indica dónde <em>detectó</em> el problema, que a menudo es la línea posterior a la que contiene el error. Revise también la línea anterior.</div>\n      <div class=\"uppgift\"><strong>Ejercicio:</strong> escribir cuatro líneas introduciéndose: nombre, edad, cosa favorita y un saludo. Luego cambiar dos líneas alrededor y ejecutarlo de nuevo. La salida cambia también: el orden en el código <em>es</em> el programa.</div>"
    },
    {
      "del": "Fundamentos",
      "titel": "Variables y tipos de datos",
      "fil": "02_variables.py",
      "kod": "# Una variable es una caja con una etiqueta en ella.\nname = \"Alex\"          # str — texto\nage = 12               # int — número entero\nheight = 1.55          # flotador — número decimal\nlikes_pizza = True     # bool — verdadero o falso\n\nprint(name, \"is\", age, \"years old and\", height, \"m tall.\")\n\n# type() le dice qué tipo de datos hay en el cuadro\nprint(type(name), type(age), type(height), type(likes_pizza))\n\n# f-string: poner f antes de la cita y soltar variables en •\nprint(f\"{name} turns {age + 1} next year.\")\n\n# Mismo símbolo, significado diferente:\nprint(3 + 4)           # 7 — aritmética\nprint(\"3\" + \"4\")       # 34 — los textos están pegados\n",
      "forklaring": "\n      <h3>Asignación</h3>\n      <p>El signo de iguales en <code>age = 12</code> no significa \"es igual a\" como en matemáticas. Significa <strong>poner lo que está a la derecha en la caja de la izquierda</strong>. Por eso <code>count = count + 1</code> funciona, lo que sería una tontería en matemáticas.</p>\n      <h3>Los cuatro tipos básicos</h3>\n      <ul>\n        <li><code>str</code> — texto, siempre entre comillas.</li>\n        <li><code>int</code> — números enteros, sin decimales.</li>\n        <li><code>float</code> — números decimales. Python escribe un punto decimal, no una coma: <code>1.55</code>.</li>\n        <li><code>bool</code> — sólo dos valores posibles, <code>True</code> y <code>False</code>La letra mayúscula, siempre.</li>\n      </ul>\n      <p>El tipo decide lo que puedes hacer. <code>+</code> entre dos números añade, <code>+</code> entre dos cuerdas pegadas, y <code>+</code> entre una cadena y un número da <code>TypeError</code> - Python se niega a adivinar a cuál te referías.</p>\n      <h3>Reglas para nombres de variables</h3>\n      <p>Letras, dígitos y subrayados, pero nunca un dígito primero. No hay espacios: escribir <code>max_score</code>, no <code>max score</code>. Las mayúsculas y minúsculas son cosas diferentes... <code>Name</code> y <code>name</code> son dos cajas separadas, y que es una fuente común de <code>NameError</code>.</p>\n      <p>Elige nombres que digan cuál es la cosa. <code>x</code> no dice nada, <code>number_of_guesses</code> Estás escribiendo un código que tu futuro yo tiene que ser capaz de leer.</p>\n      <div class=\"uppgift\"><strong>Ejercicio:</strong> crear variables para el título de una película, longitud en minutos y su calificación (1-5). Imprima una línea ordenada con una f-cadena. A continuación, calcular la longitud en horas con <code>minutes / 60</code> — ¿Cuál es la respuesta?</div>"
    },
    {
      "del": "Fundamentos",
      "titel": "Preguntar al usuario",
      "fil": "03_input.py",
      "kod": "# input() pausa el programa y espera una respuesta.\nname = input(\"What is your name? \")\nprint(f\"Hi {name}!\")\n\n# La respuesta de la entrada es SIEMPRE una cadena, incluso cuando escribe dígitos.\nanswer = input(\"How old are you? \")\nprint(type(answer))        # str\n\nage = int(answer)          # convertir el texto en un número entero\nprint(f\"In 10 years you will be {age + 10}.\")\n\n# Se puede hacer en un solo paso:\nheight = float(input(\"How tall are you in metres? \"))\nprint(f\"You are {height * 100} cm tall.\")\n",
      "forklaring": "\n      <h3>entrada pausa el programa</h3>\n      <p><code>input()</code> detiene el programa, muestra el texto que le dio, y espera hasta que el usuario responda. La respuesta se convierte en la función <em>valor de retorno</em> — lo que vuelve y lo que atrapas en una variable.</p>\n      <p>En Pythonlab la pregunta aparece como un pequeño campo en el panel de salida. En un Python normal en su ordenador está escrito en el terminal en su lugar, pero el código es idéntico.</p>\n      <h3>Conversión de tipo</h3>\n      <p>Puesto que la respuesta es siempre un <code>str</code>, usted tiene que convertirlo antes de que usted puede calcular:</p>\n      <ul>\n        <li><code>int(\"42\")</code> da todo el número 42.</li>\n        <li><code>float(\"1.55\")</code> da el número decimal 1.55.</li>\n        <li><code>str(42)</code> va al revés, al texto <code>\"42\"</code>.</li>\n      </ul>\n      <p>Leer <code>int(input(\"...\"))</code> desde adentro hacia afuera: primero <code>input</code> ejecuta, entonces su respuesta se pasa a <code>int</code>. Las funciones pueden ser anidadas dentro del otro tan profundamente como usted quiera.</p>\n      <div class=\"fallgrop\"><strong>Error común:</strong> <code>int(\"hello\")</code> produce <code>ValueError</code>, al igual que <code>int(\"3.5\")</code>: un entero no admite decimales. En el capítulo sobre gestión de errores aprenderá a capturarlo para evitar que el programa se detenga.</div>\n      <div class=\"uppgift\"><strong>Ejercicio:</strong> construir una calculadora que pide dos números e imprime la suma, la diferencia y el producto. A continuación, probarlo con letras en lugar de dígitos y leer el mensaje de error cuidadosamente.</div>"
    },
    {
      "del": "Fundamentos",
      "titel": "Condiciones",
      "fil": "04_conditions.py",
      "kod": "temperature = 17\n\nif temperature > 25:\n    print(\"Go for a swim!\")\nelif temperature > 15:\n    print(\"Bring a jumper.\")\nelse:\n    print(\"Hat on.\")\n\n# Las comparaciones siempre dan Verdadero o Falso\nprint(5 > 3, 5 == 5, 5 != 5, 5 <= 4)\n\n# y, o y no combinar condiciones\nage = 12\nhas_ticket = True\n\nif age >= 11 and has_ticket:\n    print(\"Come on in.\")\n\nif not has_ticket:\n    print(\"Buy a ticket first.\")\n",
      "forklaring": "\n      <h3>La sangría es sintaxis</h3>\n      <p>En la mayoría de los idiomas, los soportes rizados muestran lo que pertenece a ambos. <strong>sangría</strong>, y no es una cuestión de estilo, sino parte del lenguaje. Cuatro espacios en significa \"esto sólo sucede si la línea de arriba era verdad\".</p>\n      <p>La línea que abre un bloque siempre termina con un colon. <code>if</code>, <code>elif</code>, <code>else</code>, <code>for</code>, <code>while</code>, <code>def</code>, <code>try</code> y <code>class</code>.</p>\n      <h3>if, elif y otros</h3>\n      <p>Esta es la distinción más importante en todo el capítulo: <strong><code>if</code> comienza una nueva pregunta, <code>elif</code> se aferra a la anterior.</strong></p>\n      <p>En una cadena de <code>if / elif / else</code>, Python pruebas desde arriba hacia abajo y se detiene en el <em>primero</em> condición verdadera. El resto se saltan, incluso si ellos también hubieran sido verdad. Escriba dos por separado <code>if</code> declaraciones y ambas preguntas se hacen, y ambos pueden disparar.</p>\n      <p>Por lo tanto, la orden decide el resultado. <code>&gt; 15</code> antes <code>&gt; 25</code> y 30 grados se deja atrapar por el primero, así que \"¡Ve a nadar!\" nunca se alcanza. Siempre probar la condición más estrecha primero.</p>\n      <h3>Comparaciones</h3>\n      <p><code>&gt;</code> <code>&lt;</code> <code>&gt;=</code> <code>&lt;=</code> comparar el tamaño, <code>==</code> pregunta si dos cosas son iguales, <code>!=</code> si difieren.</p>\n      <div class=\"fallgrop\"><strong>Error común:</strong> <code>=</code> y <code>==</code> son completamente distintos. Un signo igual asigna un valor; dos comparan valores. <code>if x = 5:</code> es un error de sintaxis.</div>\n      <div class=\"uppgift\"><strong>Ejercicio:</strong> hacer un programa de clasificación que pide una puntuación (0–100) y respuestas A, C o F. A continuación, añadir un umbral para E y comprobar que las condiciones están en el orden correcto.</div>"
    },
    {
      "del": "Fundamentos",
      "titel": "Bucles",
      "fil": "05_loops.py",
      "kod": "# para: cuando se sabe cuántas rondas\nfor i in range(5):\n    print(\"Round number\", i)\n\nprint(\"---\")\n\n# range(start, stop, step) — la parada NO está incluida\nfor number in range(2, 11, 2):\n    print(number, end=\" \")\nprint()\n\n# mientras: cuando usted no sabe cuántas rondas\ncountdown = 3\nwhile countdown > 0:\n    print(countdown)\n    countdown = countdown - 1\nprint(\"Go!\")\n\n# Break salta, continúa saltando el resto de esta ronda\nfor number in range(10):\n    if number == 3:\n        continue\n    if number == 6:\n        break\n    print(number, end=\" \")\nprint()\n",
      "forklaring": "\n      <h3>Dos tipos de bucle</h3>\n      <p><code>for</code> camina a través de algo que ya existe —una gama de números, una lista, un pedazo de texto— y se detiene por sí mismo. <code>while</code> sigue adelante mientras una condición es verdadera, y podría en teoría girar para siempre.</p>\n      <p>Regla general: si conoce el número de rondas por adelantado, utilice <code>for</code>. Si depende de lo que sucede a lo largo del camino, utilizar <code>while</code>.</p>\n      <h3>rango</h3>\n      <ul>\n        <li><code>range(5)</code> da 0, 1, 2, 3, 4 — cinco números, pero comienzan en cero.</li>\n        <li><code>range(2, 11)</code> da 2 a 10. <strong>El valor final nunca se incluye.</strong></li>\n        <li><code>range(2, 11, 2)</code> da 2, 4, 6, 8, 10 — el tercer número es el tamaño del escalón.</li>\n      </ul>\n      <p>Contar desde cero y excluir el valor final son dos cosas que confunden a todo el mundo al principio. Se vuelven lógicas más tarde: <code>range(0, 5)</code> contiene exactamente cinco números, que mantiene la aritmética simple.</p>\n      <h3>La variable de bucle</h3>\n      <p><code>for i in range(5)</code> crea una variable <code>i</code> que obtiene un nuevo valor en cada ronda. Se puede llamar cualquier cosa — <code>for week in range(52)</code> lee mucho mejor que <code>i</code> cuando la cosa realmente es semanas.</p>\n      <h3>romper y continuar</h3>\n      <p><code>break</code> abandona el bucle por completo y continúa después de él. <code>continue</code> omite el resto de esta ronda y va directamente a la siguiente. Ambos siempre se aplican a la <em>interior</em> Loop ellos se sientan.</p>\n      <div class=\"fallgrop\"><strong>Lazo infinito:</strong> Olvidar <code>countdown = countdown - 1</code> y la condición se mantiene verdadera para siempre, congelando la página. Recargar. Si quieres probar algo arriesgado, construye en un freno de seguridad: un contador que hace <code>break</code> después de 1000 rondas.</div>\n      <div class=\"uppgift\"><strong>Ejercicio:</strong> imprimir la tabla de siete veces con un bucle for. A continuación, escribir un bucle while que sigue pidiendo números hasta que el usuario escriba 0.</div>"
    },
    {
      "del": "Colecciones",
      "titel": "Listas",
      "fil": "06_lists.py",
      "kod": "friends = [\"Sam\", \"Noor\", \"Elis\"]\n\nprint(friends[0])         # el primero — el conteo comienza a 0\nprint(friends[-1])        # el último\nprint(len(friends))       # cuántos\n\nfriends.append(\"Maya\")    # añadir al final\nfriends.insert(1, \"Kim\")  # añádase en la posición 1\nfriends.remove(\"Sam\")     # eliminar un valor particular\nprint(friends)\n\nfor friend in friends:    # bucle sobre el contenido\n    print(\"Hi\", friend)\n\nnumbers = [4, 17, 2, 9]\nprint(sum(numbers), min(numbers), max(numbers))\nnumbers.sort()\nprint(numbers)\n\nprint(\"Noor\" in friends)  # ¿Está ahí? Verdadero o Falso\nprint(friends[1:3])       # una porción de la lista\n",
      "forklaring": "\n      <h3>Una caja con compartimentos numerados</h3>\n      <p>Una lista está escrita con corchetes y contiene varios valores en un orden fijo. <strong>índice</strong>, y el primer índice es <code>0</code>. Una lista de cuatro cosas por lo tanto tiene índices 0, 1, 2 y 3 — no hay índice 4.</p>\n      <p>Los índices negativos cuentan desde el final, así que <code>[-1]</code> siempre es el último elemento. Handy cuando no sabes cuánto tiempo es la lista.</p>\n      <h3>Métodos</h3>\n      <p><code>friends.append(\"Maya\")</code> es un nuevo tipo de llamada: el punto significa \"ejecutar esta función <em>el</em> estas cosas\". Tales funciones se llaman <strong>métodos</strong>, y cuáles existen depende del tipo. Las listas tienen <code>append</code>, <code>insert</code>, <code>remove</code>, <code>sort</code>, <code>reverse</code>, <code>pop</code> y <code>count</code>.</p>\n      <p>Tenga en cuenta que <code>sort()</code> cambia la lista en su lugar y no devuelve nada. <code>numbers = numbers.sort()</code> y tú consigues <code>None</code> y perder toda la lista... un error clásico.</p>\n      <h3>Rebanadas</h3>\n      <p><code>friends[1:3]</code> da una nueva lista con los elementos del índice 1 hasta <em>pero sin incluir</em> 3. La misma regla que <code>range</code>: el valor final es exclusivo. <code>[:2]</code> significa desde el principio, <code>[2:]</code> significa hasta el final.</p>\n      <div class=\"fallgrop\"><strong>Error común:</strong> nunca elimine elementos de una lista mientras la recorre. La lista se acorta durante el bucle y algunos elementos se omiten. Cree una lista nueva en su lugar.</div>\n      <div class=\"uppgift\"><strong>Ejercicio:</strong> hacer una lista de diez números aleatorios (<code>random.randint</code>), imprimir el promedio, a continuación, construir una nueva lista que contiene sólo los números por encima de ese promedio.</div>"
    },
    {
      "del": "Colecciones",
      "titel": "Cadenas",
      "fil": "07_strings.py",
      "kod": "text = \"Python is fun\"\n\nprint(len(text))          # número de caracteres, conteo de espacios\nprint(text[0], text[-1])  # las cadenas tienen índices al igual que las listas\nprint(text[0:6])          # una rebanada\n\nprint(text.upper())\nprint(text.lower())\nprint(text.replace(\"fun\", \"logical\"))\nprint(text.split())       # se divide en una lista en los espacios\n\nanswer = \"  YES  \"\nprint(answer.strip().lower())   # recortar espacios, bajar los capiteles\n\n# Construir texto a partir de partes\nwords = [\"never\", \"give\", \"up\"]\nprint(\"-\".join(words))\n\n# Ir a través de él carácter por carácter\nfor character in \"abc\":\n    print(character, end=\".\")\nprint()\n",
      "forklaring": "\n      <h3>El texto también es una colección</h3>\n      <p>Una cadena se comporta de muchas maneras como una lista de caracteres: tiene una longitud, índices, cortes y puede ser en bucle. La diferencia es que las cadenas son <strong>inmutable</strong>. <code>text[0] = \"X\"</code> no funciona. Métodos como <code>upper()</code> Por lo tanto, no cambie el original, pero <em>devolver una nueva cadena</em> — si quieres mantener el resultado, tienes que cogerlo en una variable.</p>\n      <h3>Métodos que usará a menudo</h3>\n      <ul>\n        <li><code>strip()</code> elimina espacios y saltos de línea al principio y al final. Casi siempre lo correcto para ejecutar en lo que el usuario tecleó.</li>\n        <li><code>lower()</code> hace que todo sea más bajo, así que <code>\"YES\"</code>, <code>\"Yes\"</code> y <code>\"yes\"</code> puede ser comparado como igual.</li>\n        <li><code>split()</code> divide un texto en una lista. Sin argumento se divide en espacios, con <code>split(\",\")</code> en comas.</li>\n        <li><code>join()</code> hace lo contrario: pega una lista en una cuerda, con cualquier cosa que elijas en el medio.</li>\n        <li><code>replace(old, new)</code> cambia cada ocurrencia.</li>\n      </ul>\n      <h3>Encadenamiento</h3>\n      <p><code>answer.strip().lower()</code> ejecuta los métodos a su vez: primero se recorta el texto, luego se bajan las letras en el resultado. Lea de izquierda a derecha.</p>\n      <div class=\"uppgift\"><strong>Ejercicio:</strong> escribir un programa que pide una oración e informa el número de caracteres, el número de palabras, y la oración hacia atrás. <code>text[::-1]</code> — el paso de −1 significa \"ir hacia el otro lado\".</div>"
    },
    {
      "del": "Colecciones",
      "titel": "Diccionarios",
      "fil": "08_dictionary.py",
      "kod": "# Un diccionario combina una clave con un valor.\nstudent = {\n    \"name\": \"Elias\",\n    \"age\": 12,\n    \"subjects\": [\"maths\", \"art\"]\n}\n\nprint(student[\"name\"])\nprint(student.get(\"grade\", \"unknown\"))   # búsqueda segura con una reserva\n\nstudent[\"grade\"] = \"A\"     # añadir\nstudent[\"age\"] = 13        # cambio\n\nfor key, value in student.items():\n    print(key, \"->\", value)\n\n# Contar sucesos — especialidad del diccionario\ntext = \"banana\"\ncounts = {}\nfor character in text:\n    counts[character] = counts.get(character, 0) + 1\nprint(counts)\n",
      "forklaring": "\n      <h3>Cuando la numeración no es suficiente</h3>\n      <p>Una lista encuentra cosas por <em>posición</em>. Un diccionario los encuentra por <strong>nombre</strong>. Está escrito con tirantes rizados, y cada entrada es un par: <code>key: value</code>.</p>\n      <p>Eso hace que el código sea legible. <code>student[\"name\"]</code> dice lo que estás buscando, mientras que <code>student[0]</code> te obliga a recordar lo que significa la posición cero.</p>\n      <h3>Obtener con seguridad</h3>\n      <p><code>student[\"grade\"]</code> sobre una clave que no existe da <code>KeyError</code> y bloquea el programa. <code>student.get(\"grade\", \"unknown\")</code> da el valor de reserva en su lugar. Es por eso que el contador en la parte inferior funciona: <code>counts.get(character, 0) + 1</code> significa \"el viejo conteo, o cero si no hemos visto este personaje antes, más uno\".</p>\n      <h3>Bucleo</h3>\n      <ul>\n        <li><code>for k in student:</code> va más allá de las llaves.</li>\n        <li><code>for v in student.values():</code> pasa por encima de los valores.</li>\n        <li><code>for k, v in student.items():</code> da ambos a la vez — dos variables en el bucle, una para cada uno.</li>\n      </ul>\n      <h3>Reglas</h3>\n      <p>Las claves deben ser únicas e inmutables: las cadenas y números funcionan, las listas no. Los valores pueden ser cualquier cosa, incluidas las listas y otros diccionarios. Así es como se construyen las estructuras de datos reales: una lista de diccionarios es en la práctica una tabla.</p>\n      <div class=\"uppgift\"><strong>Ejercicio:</strong> construir una guía telefónica donde se pueden añadir nombres y números, buscar un nombre y listar cada entrada. <code>while</code> bucle con un menú.</div>"
    },
    {
      "del": "Colecciones",
      "titel": "Archivos y bases de datos",
      "fil": "09_files.py",
      "kod": "import csv\nimport json\nimport sqlite3\nimport data\n\n# ===== 1. Escribir y leer texto =====\n# \"w\" = escribir. Si el archivo ya existe está sobrescrito!\nwith open(\"note.txt\", \"w\") as file:\n    file.write(\"First line\\n\")\n    file.write(\"Second line\\n\")\n\nwith open(\"note.txt\", \"a\") as file:     # \"a\" = añadir al final\n    file.write(\"Added later\\n\")\n\nwith open(\"note.txt\") as file:          # \"r\" = leer es el valor predeterminado\n    for line_number, line in enumerate(file, 1):\n        print(line_number, line.strip())\nprint()\n\n# ===== 2. JSON: guardar listas y diccionarios como están =====\nanimals = [\n    {\"name\": \"Karo\", \"species\": \"dog\", \"weight\": 24.5, \"age\": 3},\n    {\"name\": \"Misty\", \"species\": \"cat\", \"weight\": 4.2, \"age\": 7},\n    {\"name\": \"Bruno\", \"species\": \"dog\", \"weight\": 31.0, \"age\": 5},\n    {\"name\": \"Sooty\", \"species\": \"cat\", \"weight\": 5.1, \"age\": 2},\n]\n\nwith open(\"animals.json\", \"w\") as file:\n    json.dump(animals, file, indent=2, ensure_ascii=False)\n\nwith open(\"animals.json\") as file:\n    back = json.load(file)\n\nprint(\"JSON keeps the types:\", type(back[0][\"weight\"]))\nprint(\"First entry:\", back[0])\nprint()\n\n# ===== 3. CSV: una fila por registro, se abre en Excel =====\ncolumns = [\"name\", \"species\", \"weight\", \"age\"]\nwith open(\"animals.csv\", \"w\", newline=\"\") as file:\n    writer = csv.DictWriter(file, fieldnames=columns)\n    writer.writeheader()\n    writer.writerows(animals)\n\nwith open(\"animals.csv\") as file:\n    print(file.read())\n\nwith open(\"animals.csv\") as file:\n    records = list(csv.DictReader(file))\n\n# NOTA: todo lo leído de un CSV es TEXT — convertir antes de calcular\nprint(\"CSV gives back:\", type(records[0][\"weight\"]))\nweights = [float(r[\"weight\"]) for r in records]\nprint(f\"Average weight: {sum(weights) / len(weights):.1f} kg\")\nprint()\n\n# ===== 4. sqlite3: una base de datos real =====\nconnection = sqlite3.connect(\"zoo.db\")\ncursor = connection.cursor()\n\ncursor.execute(\"DROP TABLE IF EXISTS animals\")\ncursor.execute(\"\"\"\n    CREATE TABLE animals (\n        name    TEXT,\n        species TEXT,\n        weight  REAL,\n        age     INTEGER\n    )\n\"\"\")\n\n# Los signos de interrogación son rellenados por la base de datos, nunca pegue el texto usted mismo.\nfor a in animals:\n    cursor.execute(\n        \"INSERT INTO animals VALUES (?, ?, ?, ?)\",\n        (a[\"name\"], a[\"species\"], a[\"weight\"], a[\"age\"])\n    )\nconnection.commit()        # sin cometer nada se salva\n\n# Deje que la base de datos haga el trabajo en lugar de un bucle for\ncursor.execute(\"SELECT name, weight FROM animals WHERE species = ? ORDER BY weight DESC\", (\"dog\",))\nprint(\"Dogs, heaviest first:\")\nfor name, weight in cursor.fetchall():\n    print(f\"   {name:<8} {weight} kg\")\n\ncursor.execute(\"SELECT species, COUNT(*), AVG(weight) FROM animals GROUP BY species\")\nprint(\"Summary per species:\")\nfor species, count, average in cursor.fetchall():\n    print(f\"   {species:<6} {count} of them, average {average:.1f} kg\")\n\nconnection.close()\nprint()\n\n# ===== 5. Salida y entrada al laboratorio =====\nprint(\"Files in the lab's file system:\")\ndata.list_files()\nprint()\n\n# Envíalos a tu computadora real\ndata.download(\"animals.csv\")    # funciona para cualquier archivo\ndata.download(\"animals.json\")\n\n# Traer uno de vuelta - el selector de archivos se abre, punto en animals.csv\n# (desconózcase de las líneas de abajo cuando quiera probarlo)\n# records_otrain = data.open_csv()\n# print(\"Read back\", len(records_again), \"registros\")\n# print(records_again[0])\n",
      "forklaring": "\n      <h3>Guardar significa recordar entre carreras</h3>\n      <p>Todo lo que ha hecho hasta ahora desaparece cuando el programa termina. Las variables viven en la memoria y la memoria se vacía. Un archivo se queda — y esa es la diferencia entre una calculadora y un programa que lleva un registro de algo.</p>\n      <h3>abierto y con</h3>\n      <p><code>open(\"name.txt\", \"mode\")</code> abre un archivo. Los modos son <code>\"r\"</code> leer (el valor predeterminado), <code>\"w\"</code> para escribir, y <code>\"a\"</code> para añadir al final.</p>\n      <div class=\"fallgrop\"><strong>Error común:</strong> <code>\"w\"</code> borra sin preguntar todo el contenido anterior del archivo. Si desea conservarlo, use <code>\"a\"</code>.</div>\n      <p><code>with</code> es un dispositivo de seguridad: el archivo se cierra automáticamente cuando el bloque termina, incluso si algo sale mal en el camino. Olvida cerrar y lo que escribiste puede permanecer en un buffer y nunca llegar al disco. <code>with</code>.</p>\n      <p>Los saltos de línea no aparecen por sí solos, por lo tanto <code>\\n</code> al final de cada una <code>write</code>. Y cuando lees las líneas se rompe la línea, por eso <code>.strip()</code> se utiliza al imprimir.</p>\n      <h3>Tres formatos, tres propósitos</h3>\n      <p>Ahora que conoces listas y diccionarios, la elección del formato tiene sentido, porque se reduce a qué estructura quieres preservar.</p>\n      <ul>\n        <li><strong>Archivo de texto</strong> — sólo líneas. Simple, pero tienes que interpretar el contenido tú mismo.</li>\n        <li><strong>JSON</strong> — guarda listas y diccionarios exactamente como se ven en Python, con los tipos intactos. <code>json.dump</code> escribe, <code>json.load</code> Un número sigue siendo un número después.</li>\n        <li><strong>CSV</strong> — una mesa. Se abre en Excel y en prácticamente todos los programas que hay, que es su gran fuerza.</li>\n      </ul>\n      <p><code>indent=2</code> hace que el archivo JSON sea legible para humanos, y <code>ensure_ascii=False</code> significa que los caracteres acentuados se escriben como sí mismos en lugar de como códigos.</p>\n      <div class=\"fallgrop\"><strong>Importante:</strong> todo leído desde un archivo CSV es <strong>texto</strong>, al igual que de <code>input()</code>. <code>record[\"weight\"]</code> es la cadena <code>\"24.5\"</code>, no el número. Para calcular se debe ejecutar <code>float()</code> primero. JSON no tiene ese problema, esa es toda la diferencia entre los formatos.</div>\n      <h3>csv.DictReader y diccionarios</h3>\n      <p><code>DictReader</code> devuelve un <strong>lista de diccionarios</strong> — uno por fila, con las cabeceras de columna como claves. Estas son exactamente las dos colecciones que aprendiste en los capítulos 6 y 8, y es por eso que este capítulo viene ahora: ya tienes las herramientas para trabajar con el resultado.</p>\n      <h3>sqlite3 — cuando CSV no es suficiente</h3>\n      <p>CSV funciona bien hasta unos pocos miles de filas. Más allá de eso, o si se busca y ordenar a menudo, se necesita una base de datos real. <code>sqlite3</code> barcos con Python, no necesita instalación, y almacena todo en un solo archivo.</p>\n      <p>Tres conceptos: <strong>conexión</strong> es el enlace al archivo de la base de datos, el <strong>cursor</strong> es lo que envías a través de las consultas, y <strong>commit</strong> es lo que realmente salva. Olvídese <code>commit()</code> y todo lo que escribiste se ha ido.</p>\n      <p>El lenguaje de consulta se llama SQL y es un pequeño lenguaje propio: <code>CREATE TABLE</code> crea, <code>INSERT</code> pone en, <code>SELECT</code> Lo bueno es que la base de datos hace el trabajo por ti. <code>WHERE species = 'dog' ORDER BY weight DESC</code> reemplaza un bucle con una instrucción if y un tipo — y en un millón de filas la base de datos es invariablemente más rápida que su propio código.</p>\n      <p><code>GROUP BY</code> es aún más claro: divide las filas en grupos y calcula en cada grupo por separado. Escribir que usted mismo toma un diccionario, un bucle y algún pensamiento.</p>\n      <div class=\"fallgrop\"><strong>Los signos de interrogación no son decoración.</strong> Escribir <code>execute(\"... WHERE species = ?\", (species,))</code> y dejar que la base de datos llene el valor. Pegue el texto junto con <code>+</code> y se abre el agujero conocido como inyección SQL, donde alguien puede escribir texto que se convierte en comandos. Es uno de los defectos de seguridad más comunes que hay, y el hábito de utilizar siempre signos de interrogación es la protección.</div>\n      <h3>Dos sistemas de archivos — y es importante que los mantengas separados</h3>\n      <p>Esto es lo que más confunde a la gente, así que lea lentamente.</p>\n      <p><strong>El sistema de archivos del laboratorio</strong> vive dentro del navegador. Aquí es donde <code>open()</code> escribe. Desde el punto de vista de Python se ve exactamente como un disco duro real — el mismo código, las mismas funciones — pero ninguno de ellos aparece en Explorer o Finder. Los archivos permanecen mientras la pestaña está abierta, entre carreras, y desaparecen cuando se recarga la página.</p>\n      <p><strong>Su disco duro real</strong> Python nunca llega. Una página web no puede leer o escribir en su sistema de archivos a voluntad, la misma protección que CORS, y existe para que una página que visita no pueda rebuscar en sus documentos.</p>\n      <p>Por lo tanto, se necesitan dos puentes entre los mundos, y ninguno de ellos es Python escribiendo en disco:</p>\n      <ul>\n        <li><code>data.download(\"animals.csv\")</code> lee el archivo fuera del laboratorio y entrega los contenidos al navegador, que realiza una descarga ordinaria. Lo mismo que hacer clic en un enlace de descarga.</li>\n        <li><code>data.open_file()</code> abre un selector de archivos. El archivo <em>tú</em> punto en se copia en el sistema de archivos del laboratorio y luego se puede leer con ordinario <code>open()</code>. Su elección activa es lo que otorga el permiso — el programa no puede elegir para usted.</li>\n      </ul>\n      <p>Hay atajos: <code>data.open_csv()</code> le da una lista de diccionarios directamente, y <code>data.open_json()</code> devuelve lo que se salvó. <code>data.list_files()</code> muestra lo que hay en el laboratorio ahora mismo.</p>\n      <p>También significa que usted puede tomar en <strong>cualquier CSV en absoluto</strong> — una exportación de Excel, estadísticas descargadas, un archivo de la escuela — y analizarlo con el código de este capítulo.</p>\n      <h3>En una computadora de verdad</h3>\n      <p>Sólo hay un sistema de archivos allí. <code>open()</code> escribe directamente en el disco duro, los archivos permanecen para siempre, y la base de datos sobrevive entre carreras. <code>data</code> las funciones son necesarias — existen precisamente para compensar las restricciones del navegador. Todo el otro código en este capítulo funciona sin cambios.</p>\n      <div class=\"fallgrop\"><strong>Vale la pena saber:</strong> porque los archivos viven entre carreras, los contenidos antiguos pueden permanecer y confundirte. <code>\"a\"</code> dos veces y el archivo crece. <code>data.list_files()</code> muestra lo que realmente está allí, y la recarga de la página comienza desde vacío.</div>\n      <div class=\"uppgift\"><strong>Ejercicio:</strong> construir un diario en sqlite3. Una función añade una fecha y un pedazo de texto, otra obtiene cada entrada, y una tercera búsqueda de una palabra con <code>WHERE text LIKE ?</code> y el patrón <code>\"%word%\"</code>. A continuación, exportar todo el diario a CSV y descargarlo — y la próxima vez que abra el laboratorio, volver a leerlo con <code>data.open_csv()</code> Entonces tienes un diario que realmente sobrevive.</div>"
    },
    {
      "del": "Estructura",
      "titel": "Funciones",
      "fil": "10_functions.py",
      "kod": "def greet(name):\n    return f\"Hey {name}!\"\n\nprint(greet(\"Kim\"))\nprint(greet(\"Robin\"))\n\ndef area(width, height=10):    # altura tiene un valor predeterminado\n    return width * height\n\nprint(area(4, 6))\nprint(area(4))                 # altura se convierte en 10\n\ndef is_even(number):\n    return number % 2 == 0     # devuelve Verdadero o Falso\n\nfor n in range(1, 6):\n    if is_even(n):\n        print(n, \"is even\")\n\ndef show(text):\n    print(\">>\", text)          # imprime pero no devuelve nada\n\nresult = show(\"hello\")\nprint(result)                  # Ninguno\n",
      "forklaring": "\n      <h3>Su propio botón de comando</h3>\n      <p><code>def</code> define una función. La línea termina con un colon y el cuerpo es dentado, exactamente como con <code>if</code> y <code>for</code>.</p>\n      <p>Definir no se está ejecutando. El código interior sólo ocurre cuando usted <em>Llamada</em> la función con paréntesis: <code>greet(\"Kim\")</code>. La definición debe venir antes de la llamada en el archivo.</p>\n      <h3>Parámetros y argumentos</h3>\n      <p><code>name</code> en <code>def greet(name)</code> es una <strong>parámetro</strong> — un hueco vacío. <code>\"Kim\"</code> en la llamada es un <strong>argumento</strong> — lo que llena la ranura. El parámetro sólo existe dentro de la función.</p>\n      <p><code>height=10</code> da un valor predeterminado, lo que hace que el argumento sea opcional. Los parámetros con valores predeterminados deben ser los últimos.</p>\n      <h3>volver</h3>\n      <p><code>return</code> devuelve un valor y termina la función inmediatamente — código después <code>return</code> nunca corre. Una función sin <code>return</code> devuelve <code>None</code>, que es la palabra de Python para \"nada\".</p>\n      <p>Mantener <em>impresión</em> y <em>regresar</em> aparte. <code>print</code> muestra algo a un humano. <code>return</code> da un valor de vuelta al resto del programa, que luego puede calcular con él. Los principiantes utilizan <code>print</code> donde <code>return</code> es necesario, y entonces el resultado no puede ser utilizado.</p>\n      <h3>¿Por qué funciona?</h3>\n      <p>Escribe una vez, usa muchas veces. Fija en un lugar en lugar de siete. Y sobre todo: una función con un buen nombre hace que el código sea legible — <code>if is_prime(n):</code> se explica a sí mismo.</p>\n      <div class=\"uppgift\"><strong>Ejercicio:</strong> convertir su prueba principal en una función <code>is_prime(number)</code> que devuelve <code>True</code> o <code>False</code>. A continuación, úselo en un bucle para listar cada primo por debajo de 100.</div>"
    },
    {
      "del": "Estructura",
      "titel": "Gestión de errores",
      "fil": "11_errors.py",
      "kod": "# Sin protección, el programa se bloquea con una mala entrada.\ntry:\n    age = int(input(\"How old are you? \"))\n    print(f\"In 5 years you will be {age + 5}.\")\nexcept ValueError:\n    print(\"That was not a whole number.\")\n\n# Pregunta una y otra vez hasta que la respuesta sea suficiente\nwhile True:\n    try:\n        number = int(input(\"Type a whole number: \"))\n        break\n    except ValueError:\n        print(\"Try again.\")\n\nprint(\"You typed\", number)\n\n# Diferentes errores se pueden detectar por separado\ntry:\n    print(10 / 0)\nexcept ZeroDivisionError:\n    print(\"Cannot divide by zero.\")\n\n# Compruébelo usted mismo con si — y pare con el aumento\ndef square_root(number):\n    if number < 0:\n        raise ValueError(\"negative numbers do not work\")\n    return number ** 0.5\n\nprint(square_root(16))\n",
      "forklaring": "\n      <h3>Los errores no son inusuales</h3>\n      <p>Tan pronto como un programa se encuentra con un ser humano que tendrá basura en él. Un buen programa no se estrella entonces — lo dice y pregunta de nuevo.</p>\n      <h3>tratar y excepto</h3>\n      <p>El código en el <code>try</code> block corre como de costumbre. Si ocurre un error, <code>try</code> es abandonado inmediatamente y Python salta a <code>except</code>Si nada sale mal, <code>except</code> se ha saltado por completo.</p>\n      <p>Siempre escriba qué tipo de error está capturando. <code>except ValueError:</code> captura exactamente una conversión fallida. <code>except:</code> capturas <em>todo</em>, incluyendo tus propios errores tipográficos, y luego estás ocultando bichos de ti mismo.</p>\n      <h3>Los tipos de error que ya ha encontrado</h3>\n      <ul>\n        <li><code>ValueError</code> — el tipo correcto, el contenido incorrecto: <code>int(\"hello\")</code>.</li>\n        <li><code>TypeError</code> — tipo incorrecto: <code>\"3\" + 4</code>.</li>\n        <li><code>ZeroDivisionError</code> — división por cero.</li>\n        <li><code>IndexError</code> y <code>KeyError</code> — la posición o la clave no existen.</li>\n        <li><code>NameError</code> — el nombre no existe, normalmente un error tipográfico.</li>\n      </ul>\n      <h3>subir</h3>\n      <p>Usted también puede <em>crear</em> Te equivocas a ti mismo. <code>raise ValueError(\"...\")</code> Detiene la función y señala que algo estaba mal. Mejor que devolver un valor inventado que la persona que llama podría perder.</p>\n      <div class=\"uppgift\"><strong>Ejercicio:</strong> escribir la función <code>ask_number(text, lowest, highest)</code> que sigue preguntando hasta que la respuesta es un número entero dentro del rango, y lo devuelve. A continuación, úselo en el juego de adivinanzas.</div>"
    },
    {
      "del": "Estructura",
      "titel": "Módulos",
      "fil": "12_modules.py",
      "kod": "import random\nimport math\nimport time\n\nprint(random.randint(1, 6))              # número entero, ambos extremos incluidos\nprint(random.choice([\"rock\", \"paper\", \"scissors\"]))\ncards = [1, 2, 3, 4, 5]\nrandom.shuffle(cards)                    # baraja la lista en su lugar\nprint(cards)\n\nprint(math.sqrt(144))                    # raíz cuadrada\nprint(math.isqrt(144))                   # raíz entera, exacta\nprint(math.pi)\nprint(math.floor(3.7), math.ceil(3.2))   # Redondea abajo y arriba\n\n# de ... la importación sólo trae una cosa\nfrom math import sqrt\nprint(sqrt(81))\n\nstart = time.time()\ntotal = sum(range(1000000))\nprint(\"Took\", round(time.time() - start, 3), \"seconds\")\n",
      "forklaring": "\n      <h3>Cajas de herramientas listas</h3>\n      <p>Un módulo es una colección de código terminado que alguien más escribió. Python tiene cientos incorporados — usted no tiene que inventar la raíz cuadrada usted mismo.</p>\n      <p><code>import math</code> obtiene todo el módulo, y se llega al contenido con un punto: <code>math.sqrt(9)</code>. <code>from math import sqrt</code> Obtiene sólo una función, que luego se utiliza sin el prefijo. La primera es más clara en los programas más grandes, porque se puede ver de dónde viene la función.</p>\n      <h3>Tres módulos que vale la pena conocer</h3>\n      <ul>\n        <li><code>random</code> — <code>randint(a, b)</code>, <code>choice(list)</code>, <code>shuffle(list)</code>, <code>random()</code> para un decimal entre 0 y 1.</li>\n        <li><code>math</code> — <code>sqrt</code>, <code>isqrt</code>, <code>floor</code>, <code>ceil</code>, <code>pi</code>, <code>gcd</code>.</li>\n        <li><code>time</code> — <code>time()</code> da segundos desde 1970, útil para medir el tiempo que su código toma.</li>\n      </ul>\n      <h3>Lo que funciona aquí en el laboratorio</h3>\n      <p>Pythonlab ejecuta Python dentro del navegador. Los módulos estándar están allí, pero cualquier cosa que toque el mundo exterior no lo es: no puede abrir archivos en el disco duro desde código Python, no puede navegar por la web, y no puede utilizar bibliotecas gráficas como <code>tkinter</code> o <code>pygame</code>. <code>time.sleep()</code> funciona pero congela la página mientras se ejecuta — mantenla corta.</p>\n      <p>Si quieres ir allí más tarde, necesitas que Python se instale en tu ordenador. Entonces todo el mundo se abre: juegos, gráficos, archivos, Internet.</p>\n      <div class=\"uppgift\"><strong>Ejercicio:</strong> construir piedra–papel–tijeras contra el ordenador. <code>random.choice</code>, mantener la puntuación en variables, y jugar mejor de cinco.</div>"
    },
    {
      "del": "Estructura",
      "titel": "Clases",
      "fil": "13_classes.py",
      "kod": "class Dog:\n    def __init__(self, name, age):\n        self.name = name          # las propiedades se almacenan en el objeto\n        self.age = age\n        self.fullness = 0\n\n    def bark(self):\n        return f\"{self.name} says woof!\"\n\n    def eat(self, amount):\n        self.fullness = self.fullness + amount\n        if self.fullness > 10:\n            return f\"{self.name} is stuffed.\"\n        return f\"{self.name} wants more.\"\n\n# Crear objetos de la clase\nkaro = Dog(\"Karo\", 3)\nfido = Dog(\"Fido\", 7)\n\nprint(karo.bark())\nprint(fido.bark())\nprint(karo.eat(4))\nprint(karo.eat(8))\nprint(fido.fullness)     # Fido no se ve afectado por la comida de Karo\n",
      "forklaring": "\n      <h3>Dibujar un plano, construir muchas copias</h3>\n      <p>A <strong>clase</strong> es un plano. <strong>objeto</strong> es una cosa construida a partir de ese plano. <code>Dog</code> es el modelo; <code>karo</code> y <code>fido</code> son dos perros con su propio conjunto de propiedades separadas.</p>\n      <p>Los nombres de clase están escritos con mayúsculas. Es sólo una convención, pero todo el mundo la sigue.</p>\n      <h3>__init__ y yo mismo</h3>\n      <p><code>__init__</code> se ejecuta automáticamente cuando se crea un objeto con <code>Dog(\"Karo\", 3)</code>. Llena el nuevo objeto con sus valores iniciales.</p>\n      <p><code>self</code> es el objeto en sí. Cada método lo obtiene como su primer parámetro, pero nunca lo pasas en la llamada — Python lo rellena para ti. <code>self.name</code> significa \"nombre de este objeto\", en lugar de una variable ordinaria que desaparece cuando el método termina.</p>\n      <p>Los doble subrayados marcan un nombre que tiene un significado especial para Python. Hay otros: <code>__str__</code> decide qué <code>print(karo)</code> shows.</p>\n      <h3>Cuando vale la pena el problema</h3>\n      <p>Cuando varias cosas pertenecen juntas y se comportan de la misma manera, pero con datos diferentes. Un jugador con salud, puntuación e inventario. Una cuenta con un balance y un historial. En su lugar podría utilizar diccionarios, pero luego los datos y las funciones viven en lugares separados — la clase los mantiene juntos.</p>\n      <div class=\"uppgift\"><strong>Ejercicio:</strong> Escribir una clase <code>Account</code> con los métodos <code>deposit</code>, <code>withdraw</code> y <code>balance</code>. Marca <code>withdraw</code> rechazar si no hay suficiente dinero. Crear dos cuentas y comprobar que no se afectan entre sí.</div>"
    },
    {
      "nr": 14,
      "del": "Fuentes de energía",
      "titel": "Geotermal",
      "fil": "14_geothermal.py",
      "kod": "import numpy as np\nimport matplotlib.pyplot as plt\n\ndef geothermal_temperature(depth_km, surface=15, gradient=30):\n    return surface + gradient * depth_km\n\ndef well_power_kW(mass_flow, production_C, reinjection_C, cp=4186, efficiency=0.12):\n    thermal_kW = mass_flow * cp * (production_C - reinjection_C) / 1000\n    return thermal_kW, efficiency * thermal_kW\n\ndepths = np.arange(1, 6)\ntemperatures = geothermal_temperature(depths)\nprint(\"Temperaturas (C):\", temperatures)\nprint(\"Potencia del pozo (térmica, eléctrica) kW:\", well_power_kW(80, 180, 70))\n\nfig, ax = plt.subplots(figsize=(8, 4))\nax.plot(depths, temperatures, marker=\"o\")\nax.set(xlabel=\"Profundidad (km)\", ylabel=\"Temperatura (C)\", title=\"Perfil geotérmico de temperatura\")\nax.grid(True, alpha=0.3)\nplt.tight_layout()\nplt.show()\n",
      "forklaring": "\n      <h3>Calor desde el subsuelo</h3>\n      <p>Estos ejercicios conectan el gradiente de temperatura con la potencia útil. La primera función estima la temperatura; la segunda convierte el caudal y la diferencia térmica en potencia térmica y eléctrica.</p>\n      <h3>Ejercicio</h3>\n      <p>Cambia el gradiente, encuentra la profundidad mínima que alcance 150 C y compara la salida eléctrica para varias eficiencias ORC.</p>"
    },
    {
      "nr": 15,
      "del": "Fuentes de energía",
      "titel": "Hidroeléctrica",
      "fil": "15_hydroelectric.py",
      "kod": "import numpy as np\nimport matplotlib.pyplot as plt\n\ndef hydro_power_MW(flow_m3_s, head_m, efficiency=0.90, rho=1000, g=9.81):\n    return rho * g * flow_m3_s * head_m * efficiency / 1_000_000\n\nflows = [100, 300, 500]\nheads = np.linspace(20, 200, 100)\nfor flow in flows:\n    plt.plot(heads, hydro_power_MW(flow, heads), label=f\"{flow} m3/s\")\n\nprint(f\"Salida: {hydro_power_MW(500, 120):.2f} MW\")\nplt.xlabel(\"Altura hidráulica (m)\")\nplt.ylabel(\"Potencia (MW)\")\nplt.title(\"Potencia hidroeléctrica\")\nplt.legend(title=\"Caudal\")\nplt.grid(True, alpha=0.3)\nplt.tight_layout()\nplt.show()\n",
      "forklaring": "\n      <h3>Potencia del agua</h3>\n      <p>La potencia hidroeléctrica depende del caudal, la altura hidráulica, la densidad del agua, la gravedad y la eficiencia. La función devuelve megavatios y acepta matrices de NumPy para dibujar curvas.</p>\n      <h3>Ejercicio</h3>\n      <p>Compara eficiencias del 70% al 95%, calcula energía anual usando un factor de capacidad y extiende la función con un caudal variable.</p>"
    },
    {
      "nr": 16,
      "del": "Fuentes de energía",
      "titel": "Solar",
      "fil": "16_solar.py",
      "kod": "import numpy as np\nimport matplotlib.pyplot as plt\n\ndef pv_power_kW(irradiance, ambient_C, area=100, efficiency=0.20, noct=45, alpha=-0.004):\n    cell_C = ambient_C + (irradiance / 800) * (noct - 20)\n    corrected_efficiency = efficiency * (1 + alpha * (cell_C - 25))\n    return irradiance * area * corrected_efficiency / 1000\n\nirradiance = np.linspace(0, 1200, 50)\nfor temperature in [10, 25, 40]:\n    plt.plot(irradiance, pv_power_kW(irradiance, temperature), label=f\"{temperature} C\")\n\nprint(f\"Salida del panel: {pv_power_kW(850, 25):.2f} kW\")\nplt.xlabel(\"Irradiancia (W/m2)\")\nplt.ylabel(\"Potencia (kW)\")\nplt.title(\"Salida fotovoltaica y temperatura\")\nplt.legend(title=\"Temperatura ambiente\")\nplt.grid(True, alpha=0.3)\nplt.tight_layout()\nplt.show()\n",
      "forklaring": "\n      <h3>Salida fotovoltaica</h3>\n      <p>La salida fotovoltaica aumenta con la irradiancia y disminuye cuando la célula se calienta. La función estima la temperatura de célula usando NOCT y aplica el coeficiente térmico.</p>\n      <h3>Ejercicio</h3>\n      <p>Construye un mapa de calor de irradiancia y temperatura, calcula energía anual con un factor de capacidad e incluye degradación anual.</p>"
    },
    {
      "nr": 17,
      "del": "Fuentes de energía",
      "titel": "Eólica",
      "fil": "17_wind.py",
      "kod": "import numpy as np\nimport matplotlib.pyplot as plt\n\ndef wind_power_kW(wind_speed, swept_area=5000, air_density=1.225, power_coefficient=0.45, efficiency=0.95):\n    return 0.5 * air_density * swept_area * wind_speed ** 3 * power_coefficient * efficiency / 1000\n\nspeeds = np.linspace(0, 25, 200)\npower = wind_power_kW(speeds)\nprint(f\"Potencia a 12 m/s: {wind_power_kW(12):.2f} kW\")\n\nplt.plot(speeds, power)\nplt.xlabel(\"Velocidad del viento (m/s)\")\nplt.ylabel(\"Potencia (kW)\")\nplt.title(\"Curva de potencia de un aerogenerador\")\nplt.grid(True, alpha=0.3)\nplt.tight_layout()\nplt.show()\n",
      "forklaring": "\n      <h3>Potencia eólica</h3>\n      <p>La potencia disponible del viento es proporcional al cubo de su velocidad. La función combina densidad del aire, área barrida, coeficiente de potencia y eficiencia del generador.</p>\n      <h3>Ejercicio</h3>\n      <p>Añade velocidades de arranque y parada, compara áreas de rotor y estima energía anual aplicando un factor de capacidad.</p>"
    },
    {
      "nr": 18, "del": "Exámenes", "titel": "Geotermal", "fil": "18_geothermal_quiz.py", "kod": "", "forklaring": ""
    },
    {
      "nr": 19, "del": "Exámenes", "titel": "Hidroeléctrica", "fil": "19_hydroelectric_quiz.py", "kod": "", "forklaring": ""
    },
    {
      "nr": 20, "del": "Exámenes", "titel": "Solar", "fil": "20_solar_quiz.py", "kod": "", "forklaring": ""
    },
    {
      "nr": 21, "del": "Exámenes", "titel": "Eólica", "fil": "21_wind_quiz.py", "kod": "", "forklaring": ""
    }
  ],
  "start": "\n      <p class=\"valkomst\">Bienvenido a EAGE Python SandBox</p>\n      <p class=\"ingress\">Ejecute Python real directamente en el navegador. El entorno cubre fundamentos, colecciones y estructura de programas, e incluye un espacio de proyecto vacío para Energía solar.</p>\n      <div class=\"snabbstart\">\n        <strong>Primeros pasos</strong>\n        <ol>\n          <li>Elija un capítulo de la lista de la izquierda.</li>\n          <li>Lea la explicación y revise el código.</li>\n          <li>Pulse <kbd>Ejecutar</kbd> o <kbd>Ctrl</kbd> + <kbd>Enter</kbd>.</li>\n          <li>Modifique el código y vuelva a ejecutarlo.</li>\n        </ol>\n      </div>\n      <h3>Secciones del curso</h3>\n      <p><strong>Fundamentos:</strong> salida, variables, entrada, condiciones y bucles.</p>\n      <p><strong>Colecciones:</strong> listas, cadenas, diccionarios, archivos y bases de datos.</p>\n      <p><strong>Estructura:</strong> funciones, gestión de errores, módulos y clases.</p>\n      <p><strong>Proyectos:</strong> un espacio vacío de Energía solar preparado para el material del curso.</p>\n    "
};
